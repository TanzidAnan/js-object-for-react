const numbers = [45, 36, 4, 6556, 9, 44, 1, 96,];
const student = {
    name: 'tanzid Anan',
    age: 32,
    mivies: ['king Khan', 'Dhakar mastan'],

}






const about = `my name ${student.name} my age ${student.age} has numbers ${numbers[2]}  and movise ${student.mivies[0]}`;
console.log(about)


// arrow function

const getFiftyFive = () => 55;

const addSixtyFive = (num) => num + 65;

const isEvent = (x) => x % 2 == 0;

